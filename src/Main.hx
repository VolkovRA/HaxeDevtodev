package;

import devtodev.web.Devtodev;

/**
 * Пример использования.
 */
class Main
{
    /**
     * Точка входа.
     */
    public static function main() {
        trace(Devtodev.getSdkVersion());
    }
}